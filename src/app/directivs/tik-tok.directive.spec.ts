import { TikTokDirective } from './tik-tok.directive';

describe('TikTokDirective', () => {
  it('should create an instance', () => {
    const directive = new TikTokDirective();
    expect(directive).toBeTruthy();
  });
});
